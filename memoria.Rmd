---
title: "Módulo IV. Aprendizaje Automático"
subtitle: "Práctica"
author: "DSLAB"
date: "`r format(Sys.Date(), '%d de %B de %Y')`"
output:
  bookdown::html_document2:
    toc: true  # Incluye tabla de contenidos automática
    toc_float: true  # Mantener ToC visible a la izquierda
    toc_depth: 2  # Incluir dos niveles de profundidad en ToC
    number_sections: true  # Autonumerado de secciones
    theme: flatly  # Tema Bootstrap a emplear, 
                  # se puede elegir entre las opciones por defecto de Bootstrap:
                  # default, cerulean, journal, flatly, readable, spacelab, 
                  # united, cosmo, lumen, paper, sandstone, simplex, and yeti
    code_folding: show  # Oculta el código de R, incluye un botón para mostrarlo
                        # u ocultarlo
    df_print: paged  # Utiliza paged para mostrar mejor las tablas de datos
    fig_width: 7  # Anchura por defecto de las graficas (en pulgadas)
    fig_height: 5  # Altura por defecto de las gráficas (en pulgadas)
    fig_caption: true  # Incluye pie de figura y tablas
                       # El texto del caption se incluye en el argumento fig.cap
                       # en cada chunk
link-citations: yes  # Enlaces a entradas de bibliografía al final
---


# Introducción

En esta práctica vamos a estudiar la detección de intrusiones, que puede ser modelada como un problema de clasificación binaria. Es decir, el objetivo es determinar si el tráfico de la red es un comportamiento anormal o no.


```{r librerias´, echo=TRUE, message=FALSE}
library(class)
library(dplyr)
library(ggplot2)
```
# Los Datos

El conjunto de datos a auditar consiste en una amplia variedad de intrusiones simuladas en un entorno de red militar. Se creó un entorno para adquirir datos de volcado de TCP/IP sin procesar para una red simulando una LAN típica de las Fuerzas Aéreas estadounidenses. La LAN se enfocó como un entorno real y se atacó con múltiples ataques.

Una conexión es una secuencia de paquetes TCP que comienzan y terminan en algún momento entre los cuales los datos fluyen hacia y desde una dirección IP de origen a una dirección IP de destino bajo algún protocolo bien definido. Además, cada conexión se etiqueta como *normal* o como *ataque* con exactamente un tipo de ataque específico. Cada registro de conexión consta de unos 100 bytes.

Para cada conexión TCP/IP, se obtienen 41 características cuantitativas y cualitativas de los datos normales y de ataque (3 cualitativas y 38 cuantitativas). La variable de clase tiene dos categorías
- Normal
- Anómala

```{r datos, echo=TRUE}
Train_data <- read.csv("./datos/Train_data.csv")

Test_data <-  read.csv("./datos/Test_data.csv")
```

Vamos a ver los primeros datos:
```{r datos_inicio, echo=TRUE}
Train_data
```


# EDA

En primer lugar estudiamos las variables de la base de datos:
```{r EDA1, echo=TRUE}

p2 <- Train_data %>% mutate(log10_srcbytes = log10(src_bytes+0.5)) %>%
  select(class, log10_srcbytes) %>%
  na.omit() %>%
  ggplot(aes(x=log10_srcbytes, colour=class)) +
    geom_density(lwd=2)
p2

ggplot(Train_data, aes(protocol_type)) + geom_bar()+ ggtitle("Número de conexiones por protocolo de red")


ggplot(Train_data, aes(protocol_type,fill=class))+ geom_bar()+ ggtitle("Anomalías por tipo de protocolo")

```

Analizamos la variable su_attempted. Irrelevante
```{r EDA2, echo=TRUE}

p <- Train_data %>% mutate(log10_su_attempted = log10(su_attempted+0.5)) %>%
  select(class, log10_su_attempted) %>%
  na.omit() %>%
  ggplot(aes(x=log10_su_attempted, colour=class)) +
    geom_density(lwd=2)
p

ggplot(Train_data, aes(su_attempted)) + geom_bar()+ ggtitle("Número de intentos de ejecución del comando 'sudo'")
ggplot(Train_data, aes(su_attempted,fill=class))+ geom_bar()+ ggtitle("Anomalías por número de intentos de ejecución del comando 'sudo'")
```
Análisis variable duration.
```{r EDA3, echo=TRUE}
p <- Train_data %>% mutate(log10_duration = log10(duration+0.5)) %>%
  select(class, log10_duration) %>%
  na.omit() %>%
  ggplot(aes(x=log10_duration, colour=class)) +
    geom_density(lwd=2)
p


p1 <- ggplot(data = Train_data, aes(x = log10(duration+0.5), fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = log10(duration+0.5), color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2

```
Análisis por protocol_type, podría ser relevante
```{r EDA4, echo=TRUE}
p <- Train_data %>%
  select(class, protocol_type) %>%
  na.omit() %>%
  ggplot(aes(x=protocol_type, colour=class)) +
    geom_density(lwd=2)
p

ggplot(Train_data, aes(protocol_type)) + geom_bar()+ ggtitle("Número de conexiones por protocolo de red")
ggplot(Train_data, aes(protocol_type,fill=class))+ geom_bar()+ ggtitle("Anomalías por tipo de protocolo")
```
Análisis de service TODO
```{r EDA4, echo=TRUE}

p <- Train_data %>%
  select(class, service) %>%
  na.omit() %>%
  ggplot(aes(x=service, colour=class)) +
    geom_density(lwd=2)
p

ggplot(Train_data, aes(service)) + geom_bar()+ ggtitle("x")
ggplot(Train_data, aes(service,fill=class))+ geom_bar()+ ggtitle("x")
```

Análisis de flag, podría ser relevante
```{r EDA4, echo=TRUE}
p <- Train_data %>%
  select(class, flag) %>%
  na.omit() %>%
  ggplot(aes(x=flag, colour=class)) +
    geom_density(lwd=2)
p

ggplot(Train_data, aes(flag)) + geom_bar()+ ggtitle("x")
ggplot(Train_data, aes(flag,fill=class))+ geom_bar()+ ggtitle("x")
```

Análisis src_bytes. No parece relevante
```{r EDA5, echo=TRUE}

p2 <- Train_data %>% mutate(log10_src_bytes = log10(src_bytes+0.5)) %>%
  select(class, log10_src_bytes) %>%
  na.omit() %>%
  ggplot(aes(x=log10_src_bytes, colour=class)) +
    geom_density(lwd=2)
p2

p1 <- ggplot(data = Train_data, aes(x = log10(src_bytes+0.5), fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = log10(src_bytes+0.5), color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2

```
Análisis de dst_byte. No parece relevante
```{r EDA6, echo=TRUE}

p2 <- Train_data %>% mutate(log10_dst_bytes = log10(dst_bytes+0.5)) %>%
  select(class, log10_dst_bytes) %>%
  na.omit() %>%
  ggplot(aes(x=log10_dst_bytes, colour=class)) +
    geom_density(lwd=2)
p2

p1 <- ggplot(data = Train_data, aes(x = log10(dst_bytes+0.5), fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = log10(dst_bytes+0.5), color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2
```
Análisis de land. Irrelevante.

```{r EDA7, echo=TRUE}

p2 <- Train_data %>% mutate(log10_land = log10(land+0.5)) %>%
  select(class, log10_land) %>%
  na.omit() %>%
  ggplot(aes(x=log10_land, colour=class)) +
    geom_density(lwd=2)
p2

p1 <- ggplot(data = Train_data, aes(x = log10(land+0.5), fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = log10(land+0.5), color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2

```

Análisis de wrong_fragment. Podría ser relevante.
```{r EDA6, echo=TRUE}

p2 <- Train_data %>% mutate(log10_wrong_fragment = log10(wrong_fragment+0.5)) %>%
  select(class, log10_wrong_fragment) %>%
  na.omit() %>%
  ggplot(aes(x=log10_wrong_fragment, colour=class)) +
    geom_density(lwd=2)
p2

p1 <- ggplot(data = Train_data, aes(x = log10(wrong_fragment+0.5), fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = log10(wrong_fragment+0.5), color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2
```

Análisis de urgent.Irrelevante
```{r EDA6, echo=TRUE}

p2 <- Train_data %>% mutate(log10_urgent = log10(urgent+0.5)) %>%
  select(class, log10_urgent) %>%
  na.omit() %>%
  ggplot(aes(x=log10_urgent, colour=class)) +
    geom_density(lwd=2)
p2

p1 <- ggplot(data = Train_data, aes(x = log10(urgent+0.5), fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = log10(urgent+0.5), color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2
```
Análisis de hot. Irrelevante
```{r EDA6, echo=TRUE}

p2 <- Train_data %>% mutate(log10_hot = log10(hot+0.5)) %>%
  select(class, log10_hot) %>%
  na.omit() %>%
  ggplot(aes(x=log10_hot, colour=class)) +
    geom_density(lwd=2)
p2

p1 <- ggplot(data = Train_data, aes(x = log10(hot+0.5), fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = log10(hot+0.5), color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2
```

Análisis de num_failed_logins. Irrelevante 
```{r EDA6, echo=TRUE}

p2 <- Train_data %>% mutate(log10_num_failed_logins = log10(num_failed_logins+0.5)) %>%
  select(class, log10_num_failed_logins) %>%
  na.omit() %>%
  ggplot(aes(x=log10_num_failed_logins, colour=class)) +
    geom_density(lwd=2)
p2

p1 <- ggplot(data = Train_data, aes(x = log10(num_failed_logins+0.5), fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = log10(num_failed_logins+0.5), color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2
```

Análisis de logged_in. Irrelevante
```{r EDA6, echo=TRUE}

p2 <- Train_data %>% 
  select(class, logged_in) %>%
  na.omit() %>%
  ggplot(aes(x=logged_in, colour=class)) +
    geom_density(lwd=2)
p2

p1 <- ggplot(data = Train_data, aes(x = logged_in, fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = logged_in, color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2
```

Análisis de num_compromised. Parece irrelevante. 
```{r EDA6, echo=TRUE}

p2 <- Train_data %>% mutate(log10_num_compromised = log10(num_compromised+0.5)) %>%
  select(class, log10_num_compromised) %>%
  na.omit() %>%
  ggplot(aes(x=log10_num_compromised, colour=class)) +
    geom_density(lwd=2)
p2

p1 <- ggplot(data = Train_data, aes(x = log10(num_compromised+0.5), fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = Train_data, aes(x = class, y = log10(num_compromised+0.5), color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2
```

Análisis de root_shell. Se ha  intentado 
```{r EDA6, echo=TRUE}
datos <- Train_data %>% mutate(root_shell_2 = case_when(root_shell == 0 ~ "Sí",
root_shell == 1 ~ "No"))
datos$root_shell_2 <- as.factor(datos$root_shell_2)

p1 <- ggplot(data = datos, aes(x = root_shell_2, fill = class)) +
      geom_density(alpha = 0.5) +
      scale_fill_manual(values = c("gray50", "orangered2")) +
      geom_rug(aes(color = class), alpha = 0.5) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p2 <- ggplot(data = datos, aes(x = class, y = root_shell_2, color = class)) +
      geom_boxplot(outlier.shape = NA) +
      geom_jitter(alpha = 0.3, width = 0.15) +
      scale_color_manual(values = c("gray50", "orangered2")) +
      theme_bw()
p1
p2
```


# Modelos de Aprendizaje Máquina

# Resultados

## Explicabilidad

# Evaluación

# Conclusiones

